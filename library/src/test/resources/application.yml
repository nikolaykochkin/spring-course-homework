spring:
  datasource:
    url: jdbc:h2:mem:testdb
  sql:
    init:
      schema-locations: schema.sql
      data-locations: test_data.sql
      mode: always
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true